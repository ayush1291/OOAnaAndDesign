package chap5.step1;

// Expand the inventory in cap 1 to include Mandolins also 
public class ProblemStatement {

	
	// identify what's common in Guitar and Mandolin, so that we can separate out those (hence, Instrument class)
	// also, since mandolin also have specs...So MandolinSpec
	// but Both specs have properties in common
	// so, we can have common specs class also
	
}
